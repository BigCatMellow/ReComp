-- Sharing metadata, read by tooling and the manager detail pane only.
return {
  summary = "Pokédex: A cycles data/stats/movelist pages; SELECT cycles the list numbered, A-Z, caught.",
  author = "Pokemon Gen 1 Recompilation Project",
  contact = "https://github.com/bryanthaboi/gen1recomp",
  tags = { "ui", "quality-of-life", "beginner" },
  differences = {
    changed = {
      "the Pokédex species data page cycles to a stats page then a movelist page on A",
      "stats page: base stats, BST, and evolution method + target",
      "movelist page: learned moves first, then TM/HM by machine number",
      "SELECT on the Pokédex list cycles numbered, alphabetical, and caught-only views",
    },
    added = {},
    known = {
      "a mon with no evolution skips that section",
      "a mon that knows no machines shows the learned section only",
      "the pages are species data, so they show even before the dex entry text is owned",
      "alphabetical and caught-only views hide species you have not seen",
    },
  },
  credits = {},
  compat = { engine = ">=1.0.0 <2.0.0", modApi = 2 },
}
